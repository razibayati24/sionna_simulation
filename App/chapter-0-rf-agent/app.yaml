# Chapter 0 is a pure static front-end (HTML/CSS/JS + a baked towers.json),
# so the app just needs a static file server on the port Databricks injects.
command: ["python", "-m", "http.server", "8000", "--bind", "0.0.0.0"]
